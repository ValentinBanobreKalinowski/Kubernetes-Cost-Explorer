apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.backendPostgresServiceName }}
  namespace: {{ .Values.backendNamespaceName }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.backendPostgresLabel }}
  ports:
    - port: 5432
      targetPort: 5432