apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.backendServiceName }}
  namespace: {{ .Values.backendNamespaceName }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.backendImageName }}
  ports:
    - port: 3000
      targetPort: 3000