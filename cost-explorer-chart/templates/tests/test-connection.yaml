apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cost-explorer-chart.fullname" . }}-test-connection"
  labels:
    {{- include "cost-explorer-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cost-explorer-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
