apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.frontendServiceName }}
  namespace: {{ .Values.frontendNamespaceName }}
spec:
  type: NodePort
  selector:
    app: {{ .Values.frontendImageName }}
  ports:
    - port: 8080
      targetPort: 80
      nodePort: 30080